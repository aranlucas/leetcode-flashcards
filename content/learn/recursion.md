---
title: Recursion
---

Recursion is a process in which a function calls itself as a subroutine. It is a method of solving problems where the solution depends on solutions to smaller instances of the same problem. Recursion can be thought of as a loop that repeats its own code with different inputs each time.
