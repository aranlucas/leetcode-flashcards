---
title: Union Find
questionCount: 69
---

Union Find is a data structure used to keep track of a collection of sets. It makes it easy to test whether two elements belong to the same set, and supports operations such as Union (joining two sets together) and Find (returning the set containing a given element). Union Find is used in graph algorithms, network flow algorithms, the disjoint-set data structure, and other applications.
