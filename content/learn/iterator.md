---
title: Iterator
---

An iterator is an object that enables iteration through a collection of items such as a list, array, or dictionary. It provides a way to access each element in the collection one at a time, without knowing the specific implementation of the collection. Iterators typically have methods for iterating over the collection such as next(), previous(), and hasNext().
