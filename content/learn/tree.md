---
title: Tree
---

// TODO

Set

TreeSet is a Java class that implements the Set interface. It stores its elements in a sorted tree structure, making it very efficient to find, add, and remove elements. It also allows for fast retrieval of elements in order of their natural sort order. TreeSet does not allow for duplicate elements, meaning that if you add the same element more than once, only one instance of that element will be kept.
