---
title: Radix Sort
questionCount: 3
---

Radix sort is an integer sorting algorithm that sorts objects according to the individual digits in their keys. This can be used to sort a list of strings, numbers, or other objects that can be expressed as a number. It is an in-place algorithm that makes no comparisons between elements, which makes it more efficient than other sorting algorithms.
