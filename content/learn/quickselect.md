---
title: Quickselect
---

Quickselect is a selection algorithm to find the kth least element in an unordered list. It is considered an improvement over quicksort as it required only linear time, as opposed to quicksort's average O(n log n) runtime. It works by using a modified partition process in which only a single element of the list is accessed until the desired ranking is found.
